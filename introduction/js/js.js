// Simple interaction for the profile page
document.addEventListener('DOMContentLoaded', () => {
    console.log("Mark's Profile Page Loaded Successfully!");
    
    // You can add an alert or a theme toggle here later!
});